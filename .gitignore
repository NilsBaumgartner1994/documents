# ── Secrets & credentials ──────────────────────────────────────────────────
# Never commit real credentials or private keys
.env
rclone/rclone.conf
rclone/service-account.json
# GitHub Copilot config contains OAuth tokens – never commit
copilot/github-copilot/
